includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    paths:
        - app
        - routes
    excludePaths:
        - storage
        - vendor
    database:
        connections:
            mysql:
                url: '%env(DB_URL)%'